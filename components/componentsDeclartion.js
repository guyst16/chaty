import React from "react";
import { Text, View, StyleSheet, TouchableOpacityComponent } from "react-native";
import COLORS from "../Colors"


function SendMassageButton() {
    return (
        <Text>TDS</Text>
    );
}

const Styles = StyleSheet.create({
    sendMassageButton: {
        backgroundColor: COLORS.LightPrimary
    }
});

export {SendMassageButton};